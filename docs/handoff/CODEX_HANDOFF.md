# Moved: Codex Handoff

Current Codex and teammate instructions now live in:

- `AGENTS.md`
- `.agents/README.md`
- the relevant role prompt in `.agents/`
- `docs/handoff/CONSTITUTION.md`
- `docs/motion-vision-scaffold.md` for motion or camera work

GitHub `main` and the linked Vercel deployment are the source of truth. Local dev servers may show stale UI.

This file is retained only for older handoff references.
