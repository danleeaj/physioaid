# Moved: Integration Contracts

The original integration plan contained early contract examples that no longer match the current TypeScript types.

Current integration guidance now lives in:

- `README.md`
- `AGENTS.md`
- `.agents/README.md`
- `docs/motion-vision-scaffold.md`
- live contracts in `src/types/assessment.ts`, `src/types/motion.ts`, and `src/lib/functional-tests/gates.ts`

Before opening a pull request, run:

- `npm run lint`
- `npm run build`

This file is retained only for older handoff references.
